{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python main.py",
    "restartPolicyType": "ON_FAILURE",
    "volumes": [
      {
        "mountPath": "/app/data",
        "servicePath": "/app/data"
      }
    ]
  }
}
